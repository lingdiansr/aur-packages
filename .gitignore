# makepkg 构建产物
src/
pkg/
*.pkg.tar.zst
*.pkg.tar.xz

# 下载的源码归档（源均为远程 URL，本地归档不入库）
*.tar.gz
*.zip
*.AppImage
*.AppImage.part

# 测试残留
.playwright-mcp/
# workflow 下载的 artifact 副本(commit/pr job 恢复后即删除,防止误提交)
.artifacts/

# nvchecker 运行产物
new.json
